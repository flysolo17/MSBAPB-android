<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MSBAPB" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/primary</item>
        <item name="fontFamily">@font/poppins_medium</item>
        <item name="colorOnSurface">@color/primary</item>
        <item name="colorOnPrimaryFixedVariant">@color/primary</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="shapeAppearanceSmallComponent">@style/ShapeAppearance.App.SmallComponent</item>
    </style>

    <style name="Theme.MSBAPB" parent="Base.Theme.MSBAPB" />
</resources>